#code in module1
#If statement to check if the module is being run directly
if __name__=="__main__":
    # If the module is being run directly, print a message
    print("This module is being run directly")